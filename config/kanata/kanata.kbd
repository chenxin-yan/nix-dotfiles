(defcfg
  process-unmapped-keys yes
)

(defsrc
  f1   f2   f3   f4   f5   f6   f7   f8   f9   f10   f11   f12
  grv  1    2    3    4    5    6    7    8    9    0
  tab  q    w    e    r    t    y    u    i    o    p
  caps a s d f g h j k l ;
  fn
)

(defvar
  tap-time 150
  hold-time 180
)

(defalias
  a (tap-hold $tap-time $hold-time a lctl)
  s (tap-hold $tap-time $hold-time s lsft)
  d (tap-hold $tap-time $hold-time d lalt)
  f (tap-hold $tap-time $hold-time f lmet)
  g (tap-hold $tap-time $hold-time g (multi lctl lsft lalt lmet))
  h (tap-hold $tap-time $hold-time h (multi lctl lsft lalt lmet))
  j (tap-hold $tap-time $hold-time j rmet)
  k (tap-hold $tap-time $hold-time k ralt)
  l (tap-hold $tap-time $hold-time l rsft)
  ; (tap-hold $tap-time $hold-time ; rctl)
  fnl (tap-hold $tap-time $hold-time fn (layer-toggle fn))
  caps (tap-hold $tap-time $hold-time esc (layer-while-held numbers))
)

(deflayer base
  brdn  brup  _    _    _    _   prev  pp  next  mute  vold  volu
  grv   1    2    3    4    5    6    7    8    9    0
  tab   q    w    e    r    t    y    u    i    o    p
  @caps @a @s @d @f @g @h @j @k @l @;
  @fnl
)

(deflayer fn
  f1   f2   f3   f4   f5   f6   f7   f8   f9   f10   f11   f12
  _ _ _ _ _ _ _ _ _ _ _
  _ _ _ _ _ _ _ _ _ _ _ 
  esc _ _ _ _ _ _ _ _ _ _
  fn
)

(deflayer numbers
  _   _   _   _   _   _   _   _   _   _   _   _
  (multi lalt grv) (multi lalt 1) (multi lalt 2) (multi lalt 3) (multi lalt 4) (multi lalt 5) (multi lalt 6) (multi lalt 7) (multi lalt 8) (multi lalt 9) (multi lalt 0)
  grv  1    2    3    4    5    6    7    8    9    0
  _ _ _ _ _ _ left down up right _
  _
)
